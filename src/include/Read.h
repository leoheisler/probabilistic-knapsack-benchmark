#ifndef READ_H
#define READ_H
#include <climits>
#include <iostream>
#include <sstream>
void read_knapsack_instance(std::istream& in,int&n, int& capacity, int& max_weight, int*& weights, int*& values, int& opt);

#endif // READ_H
